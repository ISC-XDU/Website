---
interface Props {
  class?: string;
  containerClass?: string;
  background?: 'default' | 'muted' | 'dark' | 'gradient';
  id?: string;
}

const {
  class: className = '',
  containerClass = '',
  background = 'default',
  id,
} = Astro.props;

const backgrounds = {
  default: 'bg-white',
  muted: 'bg-ink-50',
  dark: 'bg-ink-900 text-white',
  gradient: 'bg-gradient-to-br from-inspur-50 via-white to-inspur-50',
};

const sectionClass = `${backgrounds[background]} ${className}`;
---

<section id={id} class:list={[sectionClass, 'py-12 md:py-20']}>
  <div class:list={['container-page', containerClass]}>
    <slot />
  </div>
</section>

---
/**
 * 深色模式切换
 * - 基于 checkbox（无 JS 框架依赖）
 * - 写 <html data-theme="..."> + localStorage
 * - 视觉：经典 IO 开关（pill 轨道 + 圆形 thumb）
 * - 动态效果：整组 scaleX(-1) 镜像翻转 + thumb 滑动 + 颜色过渡
 */
---

<style>
  /* 整组镜像翻转（参考 Uiverse 原始效果） */
  .theme-toggle-label {
    transition: transform 0.4s cubic-bezier(0.68, -0.55, 0.27, 1.55);
  }
  .theme-toggle-input:checked + .theme-toggle-label {
    transform: scaleX(-1);
  }

  /* 轨道颜色 */
  .theme-toggle-track {
    transition: background-color 0.4s, border-color 0.4s;
  }

  /* thumb 已移除（用户要求只保留 IO 开关本体） */

  /* sun / moon 透明度 */
  .theme-toggle-sun,
  .theme-toggle-moon {
    transition: opacity 0.3s, transform 0.3s;
  }
  .theme-toggle-input:checked + .theme-toggle-label .theme-toggle-sun {
    opacity: 0.2;
    transform: scale(0.6);
  }
  .theme-toggle-input:not(:checked) + .theme-toggle-label .theme-toggle-moon {
    opacity: 0.2;
    transform: scale(0.6);
  }
</style>

<label
  for="theme-toggle"
  class="theme-toggle relative inline-block w-16 h-8 cursor-pointer select-none"
  title="切换深色模式"
>
  <input
    type="checkbox"
    id="theme-toggle"
    class="theme-toggle-input sr-only peer"
    aria-label="切换深色模式"
  />

  <span
    class="theme-toggle-label absolute inset-0 block rounded-full border-2 border-ink-900
           bg-white"
  >
    {/* 太阳（亮模式亮）—— 外层定位 + 内层缩放，避免 transform 链冲突 */}
    <span class="absolute left-2 top-1/2 -translate-y-1/2 w-3.5 h-3.5">
      <span
        class="theme-toggle-sun block w-full h-full rounded-full border-2 border-ink-900"
        aria-hidden="true"
      ></span>
    </span>

    {/* 月牙（暗模式亮）—— 同样嵌套 */}
    <span class="absolute right-2 top-1/2 -translate-y-1/2 w-3.5 h-1">
      <span
        class="theme-toggle-moon block w-full h-full rounded-full bg-ink-900"
        aria-hidden="true"
      ></span>
    </span>
  </span>
</label>

<script>
  function setupThemeToggle() {
    const cb = document.getElementById('theme-toggle') as HTMLInputElement | null;
    if (!cb) return;

    // 初始状态：从 data-theme 同步
    const current = document.documentElement.dataset.theme;
    cb.checked = current === 'dark';

    cb.addEventListener('change', () => {
      const isDark = cb.checked;
      document.documentElement.dataset.theme = isDark ? 'dark' : 'light';
      try {
        localStorage.setItem('theme', isDark ? 'dark' : 'light');
      } catch {
        // localStorage 不可用时静默
      }
    });
  }

  setupThemeToggle();
</script>
